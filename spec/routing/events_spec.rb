require 'rails_helper'

RSpec.describe "Events routing", type: :routing do

  # it "routes GET /events to events#index" do
  #   expect(get: "/events").to route_to("events#index")
  # end

  # it "routes GET /events/:id to events#show" do
  #   expect(get: "/events/1").to route_to("events#show", id: "1")
  # end
end
